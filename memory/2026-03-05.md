- pykrx investor flow broke (KeyError for 거래대금/거래량). Added schema mapping and fallback; still missing. Risk tag now shows '미확인' when flow missing.
- User wants weekday real-time data; Stage2 cache freshness enforced (ignore stale cache if older than prior business day).
- User prefers stock names not tickers; Geek handles Stage2 routine, Robo99/Optimus interpret.
- SRAM/Groq theme: 제주반도체 SRAM mentioned in Hana Securities report (on-device/IoT low-power SRAM), but direct Groq inference SRAM linkage is weak; treat as theme.
