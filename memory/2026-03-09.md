- pykrx KRX 데이터 응답이 빈 DataFrame으로 내려와 KeyError(시가/고가/저가/종가 컬럼 없음) 발생. 업데이트 시도해도 pykrx 1.2.4 유지, 증상 동일.
- cache_krx_daily.py에 FDR(네이버) fallback 및 상세 로깅(krx_cache_daily.err) 추가, 최대 80 티커로 제한해 성공(cached 80 rows for 20260306).
- rs_ranking.py에 pykrx 실패 시 FDR fallback 및 로깅(rs_ranking.err) 추가.
- 위 변경사항 git commit: "Add FDR fallback and logging for KRX RS".
