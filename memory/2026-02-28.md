- User requested KRX OHLCV backfill with retry sleep/backoff, progress logs, DB verify; pykrx failing due to KRX site changes. Upgraded pykrx to 1.2.4 but still empty.
- Switched backfill engine from pykrx to FinanceDataReader; FDR KRX listing also fails (JSON decode). Implemented fallback to cached ticker list alerts/ticker_cache.json (500 tickers) and running backfill successfully with progress logs; 400-day window yields ~263 trading rows per ticker.
